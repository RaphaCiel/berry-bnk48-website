# 🚀 คู่มือ Deploy เว็บไซต์ Berry BNK48 ไปที่ GitHub และ Vercel

## 📋 สิ่งที่ต้องเตรียม

- บัญชี GitHub (สมัครฟรีที่ [github.com](https://github.com))
- บัญชี Vercel (สมัครฟรีที่ [vercel.com](https://vercel.com) - ใช้ GitHub login ได้)
- Git ติดตั้งในเครื่อง (ดาวน์โหลดที่ [git-scm.com](https://git-scm.com))

---

## 📌 ส่วนที่ 1: อัปโหลดไป GitHub

### ขั้นตอนที่ 1: สร้าง Repository ใหม่บน GitHub

1. เข้าไปที่ [github.com](https://github.com) แล้ว Login
2. คลิกปุ่ม **"+"** มุมขวาบน → เลือก **"New repository"**
3. ตั้งค่า Repository:
   - **Repository name**: `berry-bnk48-website` (หรือชื่อที่ต้องการ)
   - **Description**: `Berry BNK48 Fan Website`
   - เลือก **Public** (ต้องเป็น Public ถ้าจะใช้ Vercel ฟรี)
   - ❌ **ไม่ต้อง** ติ๊ก "Add a README file"
   - ❌ **ไม่ต้อง** เลือก .gitignore
   - ❌ **ไม่ต้อง** เลือก license
4. คลิก **"Create repository"**
5. **สำคัญ!** จดหรือ copy URL ของ repo ไว้ เช่น:
   ```
   https://github.com/USERNAME/berry-bnk48-website.git
   ```

### ขั้นตอนที่ 2: เปิด Terminal/PowerShell ในโฟลเดอร์โปรเจกต์

1. เปิด **PowerShell** หรือ **Command Prompt**
2. ไปที่โฟลเดอร์โปรเจกต์:
   ```powershell
   cd c:\xampp\htdocs\berry
   ```

### ขั้นตอนที่ 3: Initialize Git และ Push ขึ้น GitHub

รันคำสั่งต่อไปนี้ทีละบรรทัด:

```powershell
# 1. สร้าง Git repository ในโฟลเดอร์
git init

# 2. เพิ่มไฟล์ทั้งหมดเข้า Git
git add .

# 3. Commit ไฟล์
git commit -m "Initial commit: Berry BNK48 Fan Website"

# 4. เปลี่ยนชื่อ branch เป็น main
git branch -M main

# 5. เชื่อมต่อกับ GitHub repository (แทน URL ด้วย URL ของคุณ)
git remote add origin https://github.com/USERNAME/berry-bnk48-website.git

# 6. Push ขึ้น GitHub
git push -u origin main
```

> ⚠️ **หมายเหตุ**: ในขั้นตอนที่ 5 ให้ใส่ URL repo ของคุณเองที่ได้จากขั้นตอนที่ 1

### ขั้นตอนที่ 4: ตรวจสอบผลลัพธ์

1. กลับไปที่หน้า GitHub repository
2. Refresh หน้า
3. ควรเห็นไฟล์ทั้งหมดของโปรเจกต์

---

## 📌 ส่วนที่ 2: Deploy ไปที่ Vercel

### ขั้นตอนที่ 1: Login Vercel ด้วย GitHub

1. เข้าไปที่ [vercel.com](https://vercel.com)
2. คลิก **"Start Deploying"** หรือ **"Sign Up"**
3. เลือก **"Continue with GitHub"**
4. อนุญาตให้ Vercel เข้าถึงบัญชี GitHub ของคุณ

### ขั้นตอนที่ 2: Import Project จาก GitHub

1. คลิก **"Add New..."** → **"Project"**
2. หา repository **"berry-bnk48-website"** แล้วคลิก **"Import"**

### ขั้นตอนที่ 3: ตั้งค่า Project

1. **Project Name**: ใช้ชื่อเดิมหรือตั้งใหม่ได้
2. **Framework Preset**: ควรตรวจจับเป็น **"Vite"** อัตโนมัติ
3. **Root Directory**: ปล่อยว่างไว้ (ใช้ค่า default)
4. **Build and Output Settings**: ปล่อย default
   - Build Command: `npm run build`
   - Output Directory: `dist`
5. คลิก **"Deploy"**

### ขั้นตอนที่ 4: รอ Deploy เสร็จ

1. Vercel จะ build และ deploy อัตโนมัติ
2. รอประมาณ 1-2 นาที
3. เมื่อเสร็จจะได้ URL เช่น:
   ```
   https://berry-bnk48-website.vercel.app
   ```

### ขั้นตอนที่ 5: เสร็จสิ้น! 🎉

- เว็บไซต์ของคุณพร้อมใช้งานแล้ว!
- สามารถแชร์ URL ให้คนอื่นดูได้เลย
- ทุกครั้งที่คุณ push code ใหม่ขึ้น GitHub, Vercel จะ auto-deploy ให้

---

## 🔧 การอัปเดตเว็บไซต์ในอนาคต

เมื่อต้องการแก้ไขเว็บไซต์แล้วอัปเดต:

```powershell
# 1. ไปที่โฟลเดอร์โปรเจกต์
cd c:\xampp\htdocs\berry

# 2. เพิ่มไฟล์ที่เปลี่ยนแปลง
git add .

# 3. Commit
git commit -m "Update: รายละเอียดการแก้ไข"

# 4. Push ขึ้น GitHub (Vercel จะ auto-deploy)
git push
```

---

## ❓ แก้ปัญหาที่อาจเจอ

### ปัญหา: `git: command not found`

- ดาวน์โหลด Git จาก [git-scm.com](https://git-scm.com/download/win)
- ติดตั้งแล้วเปิด PowerShell ใหม่

### ปัญหา: ถูกถาม Username/Password

- ใช้ GitHub Personal Access Token แทน Password
- สร้างที่: GitHub → Settings → Developer settings → Personal access tokens

### ปัญหา: Vercel build failed

- ตรวจสอบว่า `npm run build` ทำงานได้ในเครื่องก่อน
- ดู error log ใน Vercel dashboard

---

## 📝 สรุป

| ขั้นตอน           | แพลตฟอร์ม | ผลลัพธ์                 |
| ----------------- | --------- | ----------------------- |
| 1. สร้าง Repo     | GitHub    | มี repository เก็บ code |
| 2. Push Code      | Git       | code ขึ้น GitHub        |
| 3. Import Project | Vercel    | เชื่อมต่อ repo          |
| 4. Deploy         | Vercel    | ได้ URL เว็บไซต์สด!     |

**ขอให้โชคดีครับ! 🎉**
